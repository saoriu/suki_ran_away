// gameConstants.js
export const GAME_CONFIG = {
  TILE_WIDTH: 64,
  SCALE: 4, //size
  CAMERA_WIDTH: window.innerWidth,
  CAMERA_HEIGHT: window.innerHeight,
  UI_HEIGHT: 100,
  MOVE_SPEED: 1,
  baseProbability: 0.25, // of an event happening
  TILES_BUFFER: 1,
  regenerationInterval: 1000, // enerfy, call every 1000ms or 1s
};